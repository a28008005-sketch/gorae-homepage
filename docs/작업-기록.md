# 작업 기록 · 이어서 시작하기

새 창에서 이 프로젝트를 다시 잡을 때 읽는 문서입니다.
지금까지 무엇을 만들었고, 왜 그렇게 정했고, 무엇이 남았는지 정리했습니다.

최종 갱신 2026-09-15 · 브랜치 `main`

---

## 1. 무엇을 만들었나

노션의 **[프리미엄] 고래영어 학생관리** 페이지를 대체하는 학원 자체 원생관리 웹앱입니다.
빌드 도구 없이 `index.html` 하나로 열리고, GitHub Pages 에 올려 씁니다.

### 화면 (좌측 메뉴 순서)

| 화면 | 하는 일 |
|---|---|
| 대시보드 | 오늘 출석률·등록생·수강료 수납률, 미체크 출결 빠른 처리, 오늘 수업 태도, 업무 메모, 숙제 현황, 도서 반납 |
| 학생 명부 | 등록·수정·검색·필터, 상세(출결 이력·단어·숙제·도서·수강료), CSV |
| 출결 · 일일학습 | 출석/결석, 지각·외출·조퇴, **수업 태도**, 플래너·계획실천·숙제, 비고 |
| 숙제 관리 | 반 단위로 숙제 내기, 학생별 제출 체크, 제출률 |
| 단어 학습 | 단어학습앱 결과 받기(JSON·CSV·링크), 학생별 정답률·학습량, 학생 코드 |
| 도서 대여 | 도서 등록·CSV 일괄 등록, 청구기호 대여·반납, 연체, **워크시트 자동 생성** |
| 반 · 시간표 | 반 만들기, 요일·시간·담당·정원, 학생 배정, 주간 시간표 |
| 수강료 납부 | 월별 청구서 일괄 생성, 납부 처리, 미납 안내 문자, 납부확인서 |
| 통계 | 월별 출석률·태도·숙제 집계, 관심 학생, 날짜별 추이, CSV |
| 학부모 공유 | 기간별 학습 리포트를 링크로 전달, 인쇄·PDF |
| 설정 · 백업 | 학원 정보, 수강료 기본값, 클라우드 연결, 백업/복원, 예시 데이터 |

### 로그인 없이 열리는 공개 화면

- `#/report?d=...` 학부모 학습 리포트
- `#/receipt?d=...` 수강료 납부 확인서
- `#/import?d=...` 단어학습앱이 기록을 보내는 경로

데이터를 URL 안에 base64 로 넣어 전달합니다. 서버가 없어서 택한 방식입니다.

---

## 2. 어떻게 굴러가나

### 저장

기본은 **브라우저 localStorage**(`gorae-academy-v1`). 선생님이 여러 명이면 설정에서 **Supabase 클라우드**를 연결합니다.

```
화면(동기 읽기·쓰기) → Store → localStorage
                         ↓ onRecordChange
                       Sync → Cloud → Supabase records 표
```

- 화면은 지금까지처럼 동기로 동작합니다. 동기화는 `sync.js` 가 뒤에서 처리합니다.
- 내 변경: 대기열에 모아 800ms 뒤 전송. 오프라인이면 쌓아뒀다가 연결되면 보냅니다.
- 남의 변경: 15초마다 받아 병합하고 화면을 다시 그립니다.
- 충돌: 나중에 저장된 쪽이 남습니다(서버 시각 기준).
- 삭제: 물리 삭제가 아니라 `deleted: true` 표시. 그래야 삭제가 전파됩니다.

Supabase 쪽 표는 `supabase/schema.sql` 하나이고, 설치 절차는 `docs/서버형-설치안내.md` 에 있습니다.

### 워크시트

책을 빌려줄 때 독후활동지가 함께 나옵니다.

1. 책에 **워크시트 링크**가 등록돼 있으면 그 링크를 엽니다
2. **HTML 파일**이 등록돼 있으면 그 파일을 띄웁니다
3. 없으면 **책 레벨에 맞는 A4 1장을 자동 생성**합니다

레벨은 시리즈 표기 → AR/Lexile → 분류 순으로 판정하고, 엇갈리면 낮은 쪽을 택합니다.
형식은 `book-report-worksheet` 스킬의 규격을 그대로 옮겼습니다.

---

## 3. 왜 그렇게 정했나 (되돌리기 전에 읽을 것)

| 결정 | 이유 |
|---|---|
| 프레임워크·빌드 없음 | 원장님이 파일을 직접 열어 쓸 수 있어야 하고, 배포가 GitHub Pages 뿐입니다 |
| 화면은 동기, 동기화는 뒤에서 | 전 화면을 async 로 바꾸는 큰 공사를 피하면서 여러 기기 공유를 얻으려고 |
| 소프트 삭제 | 배열에서 빼면 다른 기기에 "지워졌다"가 전달되지 않습니다 |
| 수정 시각을 서버가 찍음 | 선생님들 기기 시계가 달라도 순서가 뒤집히지 않게 |
| 공개 화면은 URL 에 데이터를 담음 | 서버가 없어 학부모에게 보낼 방법이 이것뿐입니다. 길면 잘리므로 기간을 나눠 보내라고 안내합니다 |
| anon 키만 허용, service_role 거부 | 정적 사이트에 전권 키를 두면 안 됩니다. 입력 단계에서 막아 두었습니다 |
| 좌석 배치 삭제 | 반(클래스)이 생기면서 좌석이 관리 단위로서 의미를 잃었습니다. **저장된 좌석 값은 지우지 않았습니다** |
| 순회 점검 → 수업 태도 | 하루에 여러 번 순회하는 대신 그날 출결 줄에서 한 번에 기록하는 쪽이 실제 운영에 맞았습니다. 기존 기록은 자동 이관됩니다 |

---

## 4. 검증 상태

`tests/` 에 브라우저를 실제로 띄워 클릭하는 스크립트가 있습니다. 실행법은 `tests/README.md`.

| 스크립트 | 확인하는 것 | 상태 |
|---|---|---|
| `classes-attitude.test.js` | 반·시간표, 수업 태도, 삭제된 메뉴 | 11개 통과 |
| `homework-vocab-library.test.js` | 숙제, 단어 가져오기·중복 방지, 도서 대여 | 17개 통과 |
| `worksheet.test.js` | 레벨 판정, 생성, 대여 연동, 영어 전용 | 14개 통과 |
| `cloud-sync.test.js` | 두 기기 전파, 오프라인, 충돌, 세션 만료 | 19개 통과 |
| `bundle-check.js` | 단일 파일 빌드가 동작하는지 | 통과 |

### 확인하지 못한 것 (중요)

- **실제 Supabase 와 주고받는 부분.** 가짜 백엔드로만 검증했습니다. `cloud.js` 의 약 60줄이 여기 해당합니다. 원장님이 설치를 마치신 뒤 학생 한 명을 등록해 다른 기기에서 보이는지 확인해야 합니다.
- **단어학습앱 쪽 전송 코드.** 앱을 보지 못했습니다. 받는 쪽 형식과 예제만 `docs/단어학습앱-연동.md` 에 정리했습니다.

---

## 5. 남은 일 · 이어서 할 만한 것

원장님이 말씀하신 것 중 아직 안 한 것:

- [ ] **단어학습앱 양방향 연동** — 앱이 명부·반 정보를 읽어가거나, 숙제로 낸 단어 범위를 받아가게. 앱의 언어·플랫폼·서버 유무를 확인해야 시작할 수 있습니다.
- [ ] **GitHub Pages 켜기** — Settings → Pages → `main` / root. 학부모 공유 링크는 이 주소에서 만들어야 열립니다.
- [ ] **Supabase 실제 연결 확인** — 위 "확인하지 못한 것" 참고.

생각해 둔 것:

- [ ] 워크시트에 표지 이미지 넣기 (`book.wsCover` 자리는 이미 있음, 업로드 UI 만 없음)
- [ ] 학부모 리포트에 단어 학습·숙제 제출률 넣기 (지금은 출결만)
- [ ] 도서 대출 순위·다독왕 화면

---

## 6. 대화에서 정해진 사용자 선호

- 설명은 원장님이 누를 순서대로, 한국어로.
- 검증한 것과 못 한 것을 나눠서 말하기. 안 해 본 것을 했다고 하지 않기.
- 화면을 바꾸면 스크린샷으로 보여주기.
- 미리보기가 필요하면 단일 파일 빌드를 Artifact 로 올려 링크를 드리기
  (`node tools/bundle.js --demo --artifact` → `dist/artifact.html`).

---

## 7. 이전 미리보기 링크

작업 중 만들어 드린 미리보기 Artifact 주소입니다. 새 창에서 갱신하려면 이 URL 을 `url` 로 넘겨야 같은 링크가 유지됩니다.

```
https://claude.ai/code/artifact/d829beac-092d-4432-aeb4-f4a6c105a123
```
