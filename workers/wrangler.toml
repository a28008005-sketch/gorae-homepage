name = "gorae-notion-api"
main = "src/index.js"
compatibility_date = "2024-04-02"

[env.production]
vars = { ENVIRONMENT = "production" }

[env.development]
vars = { ENVIRONMENT = "development" }

# 나중에 대시보드에서 이 URL로 API 호출합니다
# https://gorae-notion-api.{your-account}.workers.dev/api/...
