# 고래영어 — 작업 규칙

## ★ 배포 규칙 (가장 중요)

**원장님이 "발행"이라고 말할 때만 홈페이지를 올린다.**

- 파일을 고치고 저장소(GitHub)에 올리는 것까지는 언제든 해도 된다
- 하지만 **실제 홈페이지 주소에 반영하는 것은 지시가 있을 때만** 한다
- "발행", "발행해줘", "올려줘(홈페이지에)" 같은 말이 있을 때만 배포한다
- 자동 배포는 모두 꺼두었다. 실수로 나가는 일이 없도록 유지할 것

| 하는 일 | 지시 없이 해도 되나 |
|---|---|
| 파일 수정 | 예 |
| `gorae-homepage`에 commit / push | 예 |
| **`gorae-site`에 push** | **아니오 — 이게 곧 배포다** |
| 홈페이지 실제 반영 (배포) | **아니오 — "발행" 지시 필요** |

`gorae-site`는 Vercel과 연결되어 있어 **push하는 순간 홈페이지가 바뀐다.**
그래서 평소 작업은 `gorae-homepage`에만 올리고, "발행" 지시가 있을 때
`gorae-site`로 옮겨 담는다.

## 저장소 세 개

| 저장소 | 내용 | 배포되는 곳 |
|---|---|---|
| `a28008005-sketch/gorae-homepage` | 홈페이지 원본 + 대시보드 워커 + 블로그 빌더 | — |
| `a28008005-sketch/gorae-site` | 소개 홈페이지 + 블로그 | Vercel |
| `a28008005-sketch/dailytest` | 학생용 앱 (듣기 · 웨일리 보카) | GitHub Pages |

**원생 정보는 어느 저장소에도 넣지 않는다.** 학생 데이터는 전부 노션에만 있다.

`gorae-homepage`는 지금 **공개** 상태다. 비공개로 돌리는 것이 규칙에 맞다
(GitHub → Settings → Danger Zone → Make private). 토큰은 올라간 적 없다.

## 폴더 구조

```
site/index.html    소개 홈페이지 (스타일·사진이 전부 이 파일 하나에 들어있다)
site/img/          사진 5장 (WebP)
site/blog/         노션에서 만들어지는 블로그 — git에 올리지 않는다
tools/blog/        노션 연동 블로그 빌더
workers/src/       대시보드 (Cloudflare Workers) — 이 파일 하나가 전부다
```

## 대시보드 (원장님용)

`workers/src/index.js` **한 파일**이 API와 화면을 모두 담는다.
Cloudflare Workers `hello-world`에 올라가 있다.

```
https://hello-world.a28008005.workers.dev/dashboard
```

**이 환경에서는 Cloudflare로 나가는 통신이 조직 정책으로 막혀 있다.**
`wrangler deploy`도, API 호출도 안 된다. 그래서 배포는 원장님이 직접
Cloudflare 편집기에 코드를 붙여넣고 Deploy를 눌러야 한다. 코드를 고쳤으면
`.txt`로 만들어 보내드린다 (`.js`는 윈도우에서 더블클릭하면 실행되어 오류가 난다).

### Cloudflare 시크릿 두 개

| 이름 | 쓰임 |
|---|---|
| `NOTION_API_KEY` | 노션 읽기·쓰기 |
| `DASHBOARD_PASSWORD` | 대시보드 접속 비밀번호 (한글 가능) |

비밀번호는 **읽기에도** 필요하다. 학생 앱이 공개 주소라 워커 주소도 알려지기
때문이다. 열린 창구는 `/api/health`와 학생 앱이 쓰는 `/api/voca/*` 뿐이다.

### 탭 13개

대시보드 · 학생 목록 · 출석 현황 · 월간 캘린더 · 영자신문 · 상담일지 · 결제 ·
과제 · 수업일지 · 알림 · 자료실 · 도서대여 · 업무메모

- **출석 현황** — 버튼으로 출석/결석을 눌러 노션에 바로 저장한다
- **월간 캘린더** — 날짜마다 출석(초록)·결석(빨강) 인원이 뜬다
- **과제** — 보카 앱에서 올라온 수행률·성취률이 맨 위에 뜬다
- 나머지 표는 **노션이 알려주는 대로 읽어 그대로** 보여준다.
  노션에서 칸을 더해도 코드를 고칠 필요가 없다

## 노션 표

| 표 | 쓰임 |
|---|---|
| 학생 | 원생 명부. `단어장코드`가 보카 앱 로그인 코드다 |
| 출석 / 상담일지 / 결제 / 과제 / 수업일지 / 알림 / 학원자료실 / 도서대여 / 업무메모 / 캘린더 | 대시보드 탭 |
| 영자신문 워크시트 마스터 목록 | `NEWSLETTERS` 저장소가 PDF를 올리는 표 |
| 보카기록 | 보카 앱이 그날 성적을 쌓는다 — 사람이 손대지 않는다 |
| 보카진도 | 보카 앱이 학생별 진도를 저장한다 — 사람이 손대지 않는다 |

## 학생용 앱 (`dailytest` 저장소)

| 앱 | 주소 |
|---|---|
| 고래 데일리 듣기 | `a28008005-sketch.github.io/dailytest/` |
| 웨일리 보카 | `a28008005-sketch.github.io/dailytest/voca/` |

**보카 앱은 클로드 아티팩트 「웨일리 보카」를 복사해 온 것이다.**
아티팩트는 바깥 서버로 데이터를 보낼 수 없고 학생이 로그인해야 열려서,
GitHub Pages로 옮겨야 대시보드와 이어진다.

원장님이 아티팩트를 고치시면 **최신본을 통째로 다시 복사하고 아래 세 군데를
다시 얹는다.** 캐릭터만 옮기지 말 것 — 다른 것도 같이 바뀌어 있다.

1. `arr.unshift(r); LS.set(K_REPORTS, ...)` 뒤 → `whaleSendReport(r)` 호출
2. `LS.set(progKey(profile.studentId), progress)` 옆 → `whaleSaveProgress` 호출
3. `</body>` 앞 → 학습코드 로그인 블록 통째로

세 자리 모두 **정확히 한 곳씩** 있어야 한다. 아니면 멈추고 확인할 것.

### 학습코드

아이들은 이름을 고르는 게 아니라 **코드**로 들어간다. 코드가 곧 신분이라
집에서 열어도 진도가 이어진다. 코드 없는 학생에게는 대시보드 `학생 목록` 탭의
발급 버튼으로 한 번에 만들어준다.

앱이 원래 갖고 있던 사람 아이콘(학생 고르기)은 **코드를 묻지 않고** 다른 아이
계정으로 들어가게 해서, 눌러도 코드 화면이 뜨도록 가로채 두었다. 학생 고르기나
새 학생 등록 화면이 뜨면 곧바로 덮는 감시도 걸려 있다. **이 잠금을 풀지 말 것.**

## 주의할 점

- `site/index.html`은 반드시 완전한 HTML 문서여야 한다.
  `<!doctype html>`과 `<meta name="viewport">`가 빠지면 휴대폰에서 글씨가 작게 나온다
- 한글 줄바꿈을 위해 `word-break: keep-all`을 쓴다
- 노션 열쇠 없이 블로그를 빌드하면 예시 글이 만들어진다. 배포 경로에서는
  `BLOG_REQUIRE_NOTION=1`로 막아두었으니 풀지 말 것
- 노션은 한 번에 100줄까지만 준다. 한 달 출석처럼 많아지는 조회는
  `notionQueryAll`로 이어받아야 뒷날짜가 조용히 빠지지 않는다
- 노션 글자 칸은 조각 하나에 2000자까지다. 진도처럼 긴 값은 나눠 담는다
- HTTP 헤더에는 한글을 담을 수 없다. 비밀번호는 인코딩해서 보낸다
- 워커는 UTC로 돈다. 날짜는 반드시 `seoulToday()`를 쓴다

## 고칠 때

대시보드를 고쳤으면 **브라우저로 실제로 돌려보고** 올린다. 문법 검사만으로는
화면이 깨진 것을 못 잡는다. Chromium과 Playwright가 이 환경에 깔려 있다
(`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`). 휴대폰 폭(390px)에서
가로로 밀리지 않는지도 함께 본다.

## 연락처

고래영어 초전캠퍼스 · 경남 진주시 초전동 1639-2 · 010-3803-8335
상담 신청: https://forms.gle/qz8bCWyDeZGTb6p59
