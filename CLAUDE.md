# 고래영어 원생관리 — 작업 지침

진주 초전동 고래영어학원의 **자체 원생관리 웹앱**입니다.
사용자는 학원 원장님이며 개발자가 아닙니다. 설명과 UI 문구는 모두 한국어로 씁니다.

## 이 프로젝트의 성격

- **빌드 도구·프레임워크·패키지 의존성이 없습니다.** 순수 HTML/CSS/JavaScript이고, `index.html` 을 더블클릭하면 그대로 열립니다. 이 제약을 깨지 마세요.
- 화면 코드는 저장소를 **동기(즉시)로** 읽고 씁니다. 클라우드 동기화는 `sync.js` 가 뒤에서 처리하므로, 화면을 만들 때 async 를 신경 쓸 필요가 없습니다.
- ES5 스타일(`var`, 함수 선언, 문자열 연결)로 통일되어 있습니다. 새 코드도 같은 스타일로 씁니다.
- 배포는 GitHub Pages. 서버가 없으므로 서버 전용 기능을 전제하지 마세요.

## 파일 구조

읽히는 순서대로입니다. `index.html` 의 `<script>` 순서가 곧 의존 순서입니다.

```
util.js          날짜·금액·CSV·base64 등 공통 유틸 (U)
store.js         모든 데이터와 CRUD (Store) — 핵심
ui.js            토스트·모달·태그·막대 (UI)
cloud.js         Supabase 어댑터 (Cloud) — 전송 계층 교체 가능
sync.js          동기화 엔진 (Sync)
auth-ui.js       로그인 화면·상태 칩 (AuthUI)
importer.js      단어학습앱 데이터 파싱 (VocabImport)
worksheet-style.js  워크시트 인쇄 CSS 문자열 (WORKSHEET_CSS)
worksheet.js     북리포트 워크시트 생성기 (Worksheet)
views/*.js       화면 — 각각 { title(), sub(), render(el) } 를 Views.<이름> 에 등록
app.js           해시 라우터 (App)
```

## 화면 추가하는 법

1. `assets/js/views/<이름>.js` 에 `Views.<이름> = { title, sub, render }` 등록
2. `index.html` 의 `<script>` 목록과 좌측 메뉴(`#/<이름>`)에 추가
3. 라우터는 자동으로 잡습니다. 없는 경로는 대시보드로 떨어집니다.

## 데이터 다룰 때 지켜야 할 것

- **모든 레코드에 `updatedAt` 을 찍습니다.** `Store` 안의 저장 함수는 `stamp()` 를 부르고 `save({kind, id})` 로 알립니다. 이 descriptor 가 있어야 클라우드로 올라갑니다.
- **삭제는 `deleted: true` 표시(소프트 삭제)** 입니다. 배열에서 빼면 다른 선생님 기기에 삭제가 전달되지 않습니다. 읽는 쪽은 `alive()` 로 거릅니다.
- 새 컬렉션을 만들면 `KINDS` 에 등록해야 동기화됩니다.
- 충돌은 **나중에 저장된 쪽이 남습니다** (서버 시각 기준).

## 워크시트

`worksheet.js` 는 학원의 북리포트 워크시트 규격(A4 세로 1장, **영어 전용**)을 코드로 옮긴 것입니다.
원본 규격은 `book-report-worksheet` 스킬에 있습니다. 활동 구성이나 레벨 표를 바꿀 일이 생기면 그 스킬을 먼저 확인하세요.

- 워크시트 안에는 **한글을 넣지 않습니다.** 학원명은 `academy.enName`, 분류는 `CATEGORY_EN` 으로 영어 변환합니다. (학생 이름 칸은 예외)
- **책 내용을 지어내지 않습니다.** 등록되지 않은 단어·인물 자리는 학생이 채우는 빈칸으로 둡니다.
- 관리 화면과 CSS 가 충돌하므로 워크시트는 **iframe 안에서** 그립니다.

## 확인하는 법

수정한 뒤에는 `tests/` 의 스크립트를 돌려 확인합니다. 방법은 `tests/README.md` 에 있습니다.
화면을 바꿨으면 스크린샷을 찍어 눈으로도 확인하세요.

단일 파일 빌드도 함께 확인합니다. 워크시트 생성기 코드 안에 `</body>` 같은 문자열이 있어서 번들러가 깨지기 쉽습니다.

```bash
node tools/bundle.js && node tools/bundle.js --demo
node tests/bundle-check.js
```

## 사용자와 이야기할 때

- 검증하지 않은 것을 했다고 말하지 않습니다. 확인한 범위와 확인하지 못한 범위를 나눠서 말합니다.
- 기능을 설명할 때는 원장님이 실제로 누를 순서대로 씁니다.
- 커밋 메시지와 문서도 한국어로 씁니다.
